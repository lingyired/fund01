# Fund01 · Mac 版 Landing Page 功能素材

> 用途：落地页文案底稿。截图 / 录像由你补齐（文中 `[截图：…]` / `[录像：…]` 即占位）。
> 视角：**仅 macOS 桌面端**，本页不提 Chrome 扩展版本。多菜单栏实例是头号卖点。

---

## 一、一句话定位（Hero）

**Fund01 —— 住在 Mac 菜单栏里的基金盯盘工具。**
每个持仓分组一个独立菜单栏，一眼看清今天赚了多少、亏了多少，不用开任何窗口。

---

## 二、Hero 区三个要点（Above the fold）

1. **一个分组，一个菜单栏**
   按你的方式分组盯盘：人工智能、海外投资、黄金……甚至一人一个分组追踪大 V 的持仓。每个分组一根独立菜单栏，互不干扰，还能单独开关。
2. **后台常驻，实时刷新**
   关掉窗口也在跑。交易时段每 60 秒刷新，红涨绿跌直接写在菜单栏上。
3. **截图即导入**
   把基金 App 的持仓截图丢给 AI，自动转成结构化持仓一键导入，代码自动纠错。

---

## 三、功能模块清单（按 Landing Page 区块排序）

### 区块 1 · 多菜单栏实例（头号卖点，重点展开）

> `[截图：菜单栏并排显示「全部 / 人工智能 / 海外投资 / 大V-老王」等分组，各自带涨跌色]`

- **每个分组，一根独立菜单栏**：把持仓按你的逻辑拆成多个分组——人工智能、海外投资、黄金，或者一人一个分组专门追踪某位大 V 的持仓。每个分组在 Mac 顶部菜单栏各占一根，互不干扰；还有一个「全部」汇总所有持仓。
- **想看哪根看哪根**：每一根菜单栏都能单独开关。在设置里取消勾选即可隐藏，需要时再打开；也可以直接把它拖出菜单栏临时收起，随时能唤回，不会被删掉。
- **一眼看懂今天赚没赚**：每根菜单栏显示两行——分组名，以及当日涨跌幅，红涨绿跌，扫一眼就知道这组今天是红还是绿。
- **排版随心**：可以把视觉重点放在「分组名」或「涨跌幅」上，适应你的阅读习惯。
- **颜色、字体、对齐都能改**：每根菜单栏的文字颜色、字体、对齐方向（靠左 / 居中 / 靠右）都能自由设置，几根并排也一眼分清谁是谁。
- **记住你的摆位**：菜单栏的顺序和你拖好的位置，重启后照样保留，不用每次重排。

> 文案建议（一句话版）：「你的基金不该挤在一根菜单栏里。Fund01 让每个分组都有自己的位置。」

---

### 区块 2 · 后台常驻 + 实时刷新

> `[截图：设置页「刷新间隔」或 badge 上的数字]`

- **常驻后台刷新**：App 在后台持续运行，关掉浮窗行情照样实时更新，不依赖浏览器插件。
- **智能分档刷新**：交易时段 60 秒 / 非交易时段 600 秒（可配置）。
- **日盘 + 夜盘自动切换**：白天刷基金、A 股指数和黄金；夜里自动转去刷美股指数和黄金夜盘，按市场作息走。
- **菜单栏徽章（badge）**：持仓总收益率直接写在菜单栏上，红涨绿跌，一瞥即知。

---

### 区块 3 · 浮窗（Popup）

> `[录像：点击菜单栏 → 弹出 680×600 浮窗，失焦自动收起]`

- **680×600 无装饰浮窗**：点击菜单栏实例弹出，干净无边框，不挡视线。
- **失焦即收起**：点击别处自动隐藏；闲置可配置时长后销毁，不占资源。
- **双行展示**：持仓支持「日盈亏百分比」或「日盈亏金额」双行显示。

---

### 区块 4 · 持仓分组管理

> `[截图：设置页「持仓分组」列表，含分组名、排序手柄]`

- **多分组**：持仓可归到多个分组（同一只基金按比例拆分到不同分组）。
- **分组排序**：拖动调整分组在菜单栏上的出现顺序。
- **三种编辑入口**：单只添加/编辑（金额 + 表单复用）、批量编辑（份额）、导入（见区块 5）。

---

### 区块 5 · AI 截图导入（差异化卖点）

> `[录像：基金 App 截图 → 粘贴给 AI → 生成 JSON → Fund01 导入结果页（含自动纠错提示）]`

- **截图即持仓**：把天天基金 / 支付宝等 App 的持仓截图交给 AI，自动识别成严格 JSON 数组，直接导入 Fund01。
- **代码自动纠错**：AI 同时输出基金名称，导入时按名称交叉校验 6 位代码——代码错、名字对时自动纠正并提示；两者都对不上则该条判失败、不污染数据。
- **金额口径严谨**：自动判断「资产是否已含当日收益」（`amountBasis: today / prev`）并优先锚定净值日期，避免隔夜导入把份额整体错算一天的涨跌幅。
- **失败可定位**：导入结果逐条列出成功/失败，失败项给出明确原因（如「代码与名称对不上，请核对」），不静默丢弃。

> 文案建议：「再也不用手动抄代码、算份额。截一张图，剩下的交给 AI。」

---

### 区块 6 · 指数 / 市场面板

> `[截图：popup 内指数面板，含黄金 AU9999 / XAU / COMEX]`

- **多指数勾选**：popup 与设置双界面均可独立勾选关注指数。
- **黄金三看板**：AU9999 / XAU / COMEX 黄金行情，一眼看懂金价是涨是跌。
- **自选指数条**：菜单栏 / 顶栏最多容纳 5 个自选指数，常驻可见。

---

### 区块 7 · 数据准确性承诺（信任护城河）

- **数字准确可核对**：所有涨跌幅、收益额都与官方净值口径严格对齐，绝不拿滞后涨幅冒充当日收益；估值口径在设置里对你公开，不留黑箱。
- **QDII 披露日口径**：QDII 按「净值披露日」对齐普通基金，盘中显示「-」而非用滞后涨幅冒充当日收益，净值日期恒标注在次行。
- **兜底透明**：自算估值失败时按基金类型分流（非 QDII 回退官方分时估值，QDII 保持「-」），规则在设置页对用户公开。

---

### 区块 8 · 体验与系统要求

- **主题**：深 / 浅 / 跟随系统三主题，默认跟随系统。
- **最低系统**：macOS 13.0 或更高。
- **芯片支持**：Apple Silicon 与 Intel 均支持，轻量单文件安装。
- **定位**：个人小工具，非商业化，数据本地存储。

---

## 四、给落地页的「卖点排序」建议（优先级）

| 优先级 | 卖点 | Landing Page 位置 |
|---|---|---|
| ★★★ | 多菜单栏实例（每分组一根） | Hero + 首屏大图 |
| ★★★ | AI 截图导入 | 第二屏，配录像 |
| ★★ | 后台常驻实时刷新 + badge | 功能区块 |
| ★★ | 持仓分组 / 排序 / 双行 | 功能区块 |
| ★★ | 指数 / 黄金面板 | 功能区块 |
| ★ | 跨端一致性 / 准确性承诺 | 信任区 |
| ★ | 主题 / 系统要求 | 页脚 |

---

## 五、可直接用的短标语（Slogan 备选）

- 「你的基金，本该各就各位。」
- 「每根菜单栏，管一组钱。」
- 「截图就能导入，代码自动纠错。」
- 「关掉窗口，行情还在跑。」
- 「红涨绿跌，一眼到底。」
